pipeline {
    agent any

    stages {
        stage('SCM') {
            steps {
               git 'https://github.com/Arvind22cse/Devops_Guivi.git'
            }
        }
            stage('Build'){
                steps {
                    bat 'mvn clean install'
                }
            }
            stage('build to images'){
                steps {
                    bat 'docker build -t arvindm2004/mysimplewebapplication .'
                }
            }
            stage('docker push hub'){
                steps{
                    script{
                        // This step should not normally be used in your script. Consult the inline help for details.
withDockerRegistry(credentialsId: 'docker_cred', url: 'https://index.docker.io/v1/') {
    bat 'docker push arvindm2004/mysimplewebapplication'
    // some block
}
                    }
                    
                }
            }
    }
}
